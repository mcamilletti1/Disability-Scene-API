build:
  steps:
    - name: Install Dependencies
      script:
        - pip install -r requirements.txt

deploy:
  steps:
    - name: Start Gunicorn
      script:
        - gunicorn disabilityscene_django.wsgi:application